package main

import "fmt"

func main() {
	/*
			var x int
			fmt.Print("請輸入一個數字:")
			fmt.Scanln(&x)
			fmt.Print("你輸入的數字是:", x)
		var x int
		var y int
		fmt.Print("請輸入你想要相加的兩個數字")
		fmt.Scanln(&x, &y)
		fmt.Println(x, "*", y, "=", x*y)
	*/
	var result bool = !true
	fmt.Println(result)
	fmt.Println(true && false)
	fmt.Println(true || false)
}
